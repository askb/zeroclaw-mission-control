# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2026 The Linux Foundation
#
# ZeroClaw Caddy Reverse Proxy
# Terminates TLS and proxies to gateway + mission-control services.

# ── HTTPS (primary) ──────────────────────────────────────────────
localhost {
	tls internal

	# Gateway: WebSocket connections
	handle /ws* {
		reverse_proxy gateway:18789
	}

	# Gateway: REST API
	handle /api* {
		reverse_proxy gateway:18789
	}

	# Gateway: health endpoint
	handle /health {
		reverse_proxy gateway:18789
	}

	# Mission Control dashboard (default/fallback)
	handle {
		reverse_proxy mission-control:4000
	}
}

# ── HTTP → HTTPS redirect ────────────────────────────────────────
# Disabled: ports 80/443 occupied by other services on this host.
# Caddy listens on container :443, mapped to host :8443.
# Access via: https://<host>:8443/
