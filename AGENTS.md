# AI Agents Guide — ZeroClaw Mission Control

> ⚠️ **Constitution takes precedence.** Read `.specify/memory/constitution.md`
> first. If anything in this file conflicts with the Constitution, the
> Constitution wins.

This document helps AI coding assistants (GitHub Copilot, Claude Code,
OpenClaw agents, etc.) understand this repository and work effectively within it.

## Governance Hierarchy

1. **Constitution** (`.specify/memory/constitution.md`) — Supreme rules
2. **This file** (`AGENTS.md`) — Development guidelines
3. **Copilot Instructions** (`.github/copilot-instructions.md`) — IDE-specific rules
4. **Agent Definitions** (`agents/*/agent.md`) — Per-agent behavioral rules
5. **Tool Definitions** (`agents/*/tools.md`) — Per-agent tool permissions

## Spec-Driven Development

Before making changes, check for active specs:

```bash
ls specs/           # Active feature specs
ls .specify/        # Templates and governance
```

### Workflow
1. **Read Constitution** — Understand boundaries
2. **Check specs/** — See if a spec exists for your task
3. **Read repo governance** — AGENTS.md, .github/copilot-instructions.md
4. **Analyze** — Understand current state
5. **Propose** — Present changes for approval
6. **Branch + PR** — Never push to main

## What Is This?

ZeroClaw Mission Control is a **zero-trust agent orchestration platform** that
runs specialized AI agents (researcher, coder, orchestrator) behind a secure
gateway with token-based authentication, network isolation, and Redis-backed
session management.

## Repository Map

```
.
├── docker/                    # Hardened Docker Compose setup
│   ├── docker-compose.yml     # Gateway + Dashboard + Redis
│   └── .env.example           # Secret template (NEVER commit .env)
├── config/
│   ├── zeroclaw.json5         # Gateway routing + agent config
│   └── redis.conf             # Redis security settings
├── agents/                    # Agent persona definitions
│   ├── research-specialist/   # Web search, Arxiv, citation
│   ├── code-architect/        # Filesystem, shell, GitHub CLI
│   └── orchestrator/          # Task delegation, coordination
├── scripts/
│   ├── setup.sh               # Bootstrap (token generation)
│   ├── rotate-secrets.sh      # Credential rotation
│   ├── health-check.sh        # Service verification
│   └── teardown.sh            # Clean shutdown
└── .github/workflows/         # CI/CD pipelines
```

## Security Model

This repo follows **zero-trust principles**:

| Layer | Protection |
|-------|-----------|
| Secrets | `.env` gitignored; tokens generated by `openssl rand` |
| Network | Bridge network; Redis internal-only (no host port) |
| Containers | Non-root, read-only FS, no-new-privileges |
| Resources | CPU/memory limits on all containers |
| Supply chain | All GHA actions SHA-pinned; Dependabot enabled |
| Detection | `detect-secrets` pre-commit hook; weekly security audit |
| Rotation | `rotate-secrets.sh` for credential cycling |

## When Working on This Repo

### DO ✅
- Run `pre-commit run --all-files` before committing
- Add SPDX headers to all new files
- SHA-pin any new GitHub Action references
- Include health checks on new Docker services
- Set resource limits on new containers
- Use `${ENV_VAR}` for any credential reference
- Sign commits (`git commit -s`)

### DON'T ❌
- Hardcode any secret, token, password, or API key
- Expose Redis port to the host
- Run containers as root
- Use `--no-verify` to bypass pre-commit
- Use floating tags (`@v4`) for GitHub Actions
- Add `chmod 777` or `sudo` to scripts
- Commit `.env` files

## Agent Modification Guide

When editing agent personas (`agents/*/agent.md` or `agents/*/tools.md`):

1. **Maintain separation of concerns** — Researcher researches, coder codes,
   orchestrator delegates
2. **Least privilege** — Only grant tools the agent strictly needs
3. **Document changes** — Update the agent's constraints section
4. **Test delegation** — Verify handoff paths still work
